### What is html ?

```
Hypertext markup language. Skeleton of web page
```

### Syntax

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body></body>
</html>
```

### What are html tags

```
HTML tags are like keywords which defines that how web browser will format and display the content. With the help of tags, a web browser can distinguish between an HTML content and a simple content. HTML tags contain three main parts: opening tag, content and closing tag. But some HTML tags are unclosed tags.
```

### Anchor tag

```html
<a href="" target="">click here</a> 

<!--
link to another page or website 
paste url at herf="htps://www.google.com"
'target' -> decides whether to open link in same tab or in new tab default same tab, to open in new tab set the value as _black
-->
```
